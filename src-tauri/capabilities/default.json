{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the control window is allowed to do.",
  "windows": ["control"],
  "permissions": ["core:default"]
}
